{
  "name": "inventory-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/inventory-service/src",
  "projectType": "application",
  "tags": ["scope:backend"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/inventory-service"],
      "options": { "command": "nest build", "cwd": "services/inventory-service" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": { "command": "node ../../dist/inventory-service/main.js", "cwd": "services/inventory-service" }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": { "lintFilePatterns": ["services/inventory-service/src/**/*.ts"] }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/inventory-service"],
      "options": { "jestConfig": "services/inventory-service/jest.config.js", "passWithNoTests": true }
    }
  }
}
